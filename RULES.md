# ¥100万 ルール運用bot — ルールと改良ポリシー

このbotは「割安 × 財務の質」で選んだ日本株を **100株（1単元）単位** で仮想運用し、
毎日の推奨アクションを出す。**ペーパー（仮想）運用であり、実発注はしない／投資助言でもない。**

## 現行ルール（v2・2026-06-13〜）

| 項目 | 内容 |
|---|---|
| 買い候補 | 割安スコアで「厳選」(score ≥ strict_min かつ 警告フラグなし) かつ ROE ≥ 8% を、ROE降順（質のティルト） |
| 売買単位 | 100株の整数倍のみ（実際に発注できる形） |
| 銘柄数・配分 | 最大10銘柄。1銘柄の目安予算 = 総資産 ÷ 10（≒10万円）。100株で予算超過の高株価銘柄はスキップ。余りは上位保有へ単元追加、それでも余れば現金 |
| リバランス | 28日以上あけて（≒月1回）。上位10から外れた保有を売り、上位で埋め直す |
| 売り | 日次 −25% で暴落ストップ／最長365日で入替／それ以外はリバランス主導 |
| コスト | 売買ごと 0.1%（往復0.2%）控除 |
| 価格 | 購入金額 = 実株価(Close)、評価 = 分割配当調整済(Adj Close) のトータルリターン |
| ベンチマーク | 同額を日経平均(^N225)に投じた場合 |

パラメータの実体は `portfolio.py` 冒頭の定数（START_CAPITAL / N_HOLD / FEE / DISASTER /
MAX_HOLD_DAYS / REBAL_MIN_DAYS / ROE_FLOOR）。

## 改良の進め方（＝過剰最適化を避けるための決めごと）

「随時改良したいが、過剰最適化（オーバーフィット）はしない」を守るための運用ルール。

1. **前向き適用のみ。** ルール変更は必ず「変更した日以降」だけに効かせる。過去のシミュ結果を
   後から書き換えない。→ 過去の成績が都合よく良くなる現象を構造的に封じる。
   （既存versionの定数は編集せず、新ルールは新versionとして未来の適用日で導入する）
2. **根拠は要因分析（前向き）。** 変更は `analyze_signals.py` の signed IC・分位・勝率など
   “判定→その後のリターン”の実測に基づく仮説のみ。過去リターン最大化を目的にしない。
3. **1回の変更は小さく・少なく。** 同時に複数パラメータをいじらない。効果は次月以降の
   フォワード結果で検証する（バックテスト上の見栄えでは判断しない）。
4. **単純さを優先。** 指標・条件を増やすほど過学習しやすい。新パラメータの追加は、
   明確な因果と十分な標本（目安 n ≥ 25〜）があるときだけ。迷ったら入れない。
5. **承認制。** 月次レビュー(`monthly_review.py`)が改良候補を根拠付きで提案するが、
   **自動では反映しない。** ユーザーが承認して初めて次versionとして導入する。
6. **地雷を疑う。** 「効きすぎ」な指標・特定銘柄依存・少数の当たりが牽引、は過学習のサイン。
   除外・分位の安定性・複数期間での再現を確認してから採用する。

## 現状の所見（暫定・単一期間）

要因分析の初期所見では、52週安値からの位置・ROE・配当が超過リターンと順相関、PBRは逆相関の
傾向。ただし単一・短期間の標本のため断定しない。**当面は触らずデータを貯め、月次レビューの
提案を承認制で少しずつ取り込む。**
